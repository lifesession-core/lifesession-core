{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lifesession.dev/schemas/decision.schema.json",
  "title": "Decision",
  "type": "object",
  "required": ["id", "session_id", "title", "outcome", "created_at"],
  "properties": {
    "id": {"type": "string"},
    "session_id": {"type": "string"},
    "title": {"type": "string"},
    "outcome": {"enum": ["approved", "rejected", "deferred", "needs_review"]},
    "status": {"enum": ["proposed", "approved", "rejected", "needs_review"]},
    "summary": {"type": "string"},
    "rationale": {"type": "string"},
    "created_by": {"type": "string"},
    "created_at": {"type": "string", "format": "date-time"},
    "evidence": {"type": "array", "items": {"type": "string"}}
  },
  "additionalProperties": false
}
